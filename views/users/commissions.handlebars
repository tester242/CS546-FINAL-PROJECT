{{#if errorExists}}
    <p id="error">{{error}}</p>
{{/if}}

<article>
    <p>These are all of the active commission requests accepted by Maggie.</p><br><br>
    {{#each commissions}}
        <div class="commission">
            <dl> 
                <dt>Commission ID</dt> 
                <dd>{{this.commissionId}}</dd><br>
                <dt>Status</dt> 
                <dd>{{this.status}}</dd><br>
                <dt>Price</dt> 
                <dd>{{this.price}}</dd><br><br>
            </dl>

            <form method="POST" id="commissions-form" action="/commissions">
                <button type="button">Update Status</button><br><br><br>
            </form>
        </div>
    {{/each}}
</article>


<footer>
    <a href="/commissions">Back to Top</a><br>
    <a href="/artworks">Back to All Artworks</a>
</footer>